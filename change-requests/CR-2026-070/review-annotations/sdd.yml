cr-id: CR-2026-070
review-type: tech-design
reviewer: "OldBoy405"
reviewed-at: "2026-09-18T10:43:02+08:00"
verdict: pass
blockers: []
dimensions:
  PRD↔SDD对齐: "通过（上一轮本维度因 B-1 判不通过，本轮转通过）——FR-1（AC-1～AC-4）→ §3.2 逐字文本 + §4.3 单点合成 + §4.4 重放设计；FR-2（AC-5～AC-8、AC-12）→ §3.1 契约与边界值表 + §4.1 默认值 + §4.2 秒数传递 + §4.6 取证算法；FR-3 → §1.1 的 I1～I8 与 §6.4 核对清单；§6.2 表本轮实读 12 行（AC-1～AC-12），每条均有设计落点、可观测结果与可达性说明，编号与 FR 归属与 PRD 逐条一致；PRD §1.5 三条延后项由 SDD-CLOSE-01～03 关闭且结论与 PRD 契约一致（CLOSE-03 按 PRD §7 第 11 条把路线选择留给 write-dev-plan）。批准范围前置（CR-2026-057 FR-5／AC-5）本轮逐条复核：§9 四字段齐备（scope_in 5 项、scope_out 6 项、zero_diff Z-1～Z-8、follow_up F-1～F-5）；判据 ① 唯一同类冲突（B-1）已消除（Z-5 与 §6.4 对 dep-4／dep-8 落点显式 carve-out，§6.3 的 dep-13 为唯一裁决口径）；② 工程纪律 10 强制的 CUSTOM.md 登记已进 scope_in 第 3 项、不受 zero_diff 约束（§6.4 零改动清单中无 dep-9）；③ scope_out 六项未隐藏当前必须发生的治理修改；④ F-1～F-5 均非任何当前 AC 的必要条件（F-2 本轮已收窄为只承载 repositories 声明变更）。本轮修订只动 §3.2 限定、§4.2 信号取值、§4.3 第 4 条、§6.2 的 AC-1 行、§6.3 的 dep-13、§6.4、§6.5、§7.3、§9 的 Z-5／F-2，未触碰 FR／AC 合同与 dep-N 五要素；prd.md 自 dep-1 的 54f69aae 起逐字未变（本轮复算 blob f7a19ce6…、34769 B、LF-only sha256 8972e5c5…，等于需求评审 subject-sha256）。"
  架构合理性: "通过（本轮无架构面改动，快速复核）——DEC-1～DEC-4 的三判据与替代方案未变，DEC-4 的选定措辞已与 §4.2 的新信号取值同步。本轮复测落点唯一性：multica@59b47993 的非测试代码中输出 `## Skills` 段头的仍只有 runtime_config_sections.go:838（writeSkills 定义 :833、调用 :1058）；检索锚短语在 multica worktree 与 tools worktree（均排除 .git 与 node_modules）命中数实测均为 0，故「不存在既有等价且单一的公共 Agent 行为 contract」与「当前不存在第二份复制」两侧证据同时成立。DEC-2 的共享解析点前提复测成立：dist/core/tools/bash.js 的 resolveTimeoutMs 只在 :14 定义、:38 一处调用、文件导出面不含该符号，PowerShell 经 createLocalShellOperations／createShellToolDefinition 复用同一解析点与同一 bashSchema。§1.1 的 I1～I8 与 multica ARCHITECTURE.md §5 硬不变量仍不冲突，单元 A 落在 Skill 交付边界内、不新增跨层依赖。"
  数据模型完整性: "通过（本轮无改动，条件章节 N/A 有据）——§2.1／§2.2／§2.4／§2.5 与上一轮一致：不新增数据库表／列／索引、迁移、sidecar、账本字段、metrics、配置键、环境变量、错误码枚举与 CR 状态；两个新数据形状（writeSkills 内的固定常量文本、DEFAULT_TIMEOUT_MS = 300_000 与 timeout 的 schema 说明文案）与 §3.1／§4.1／§4.2 逐处一致（§4.2 本轮只改信号的取值表达式，未新增数据形状或字段），§2.4 的无 DB schema／迁移／写路径鉴权改动的 N/A 理由成立。"
  接口契约: "通过（§4.2 修订后复核）——§3.1 的四档判定顺序、边界值表与 §4.1 算法互为可机械核对面，本轮未变；§4.2 的新信号值使工具层从 err.message.split(冒号) 取秒数的既有逻辑（bash.js:255-257）得到的字符串与现状逐字相同：显式值即调用方原始入参本身（不经 v*1000/1000 的二次浮点往返），未传时 DEFAULT_TIMEOUT_MS / 1000 精确为 300，故 NFR-1 的显式调用逐字不变在全值域字面成立；§3.5 错误语义零新增、既有错误文本逐字沿用；§3.4 无新增或修改 HTTP／IPC／事件／CLI 契约，条件基线不触发。"
  多仓架构约束: "通过——§1.3 的路径 authority（resources[].worktreePath 原样值，禁 .rayai-worktrees 目录名拼接与主 checkout 回退）本轮实测一致；crctl workspace inspect 三仓 classification 全 healthy、dirty=false；multica HEAD 59b47993810fabd12fcc393c2fa2e46611f9530d、tools HEAD c3e7c934ef2636c56cc840543cadb4feb5f554aa、KB HEAD 3c80c24d… 与 §6.3 表中 dep-3～dep-9、dep-13（multica）与 dep-10／dep-12／dep-14（tools）的 40 位 SHA 全等，14 条 dep 五要素齐备、无未定义引用；dep-11 的 c1db7b9e 存在且 c1db7b9e..HEAD 只含 cr.md／sdd.md／review-annotations/sdd.yml／review-loop.yml／traceability.yml（dir-graph.yaml 零 diff）；dep-1 的 54f69aae 与 dep-2 的 c7fde42b 均存在，且前者的 prd.md blob = f7a19ce6… = 当前 blob；单元 B 的落点仓不在 repositories 集合内（DEC-3）不变，跨仓依赖方向（multica 规则文本流向 Runtime 配置，不反向）不变。"
  性能与安全: "通过——§7.2 的六个控制面零改动声明与本 CR 改动面（multica 文本合成、Pi 数值分支）无交集，默认值仍被定位为可用性护栏而非安全边界，V-6 的安装树痕迹保持不清理、不依赖、不作为设计前提；§7.3 第 1 条的数值本轮订正为实读值（本轮实测 §3.2 逐字文本行 = 108 个空白分隔词、107 个含字母词，与文字一致），结论（相对该段既有体积为个位数百分比、缓存前缀稳定性不受影响）不变；§7.4 的 R-1～R-6 及各自出口（F-4、F-1、F-3）未被假设掉。"
  可测试性: "通过（上一轮本维度因 B-1 判不通过，本轮转通过）——AC-1 的取证面已自洽：落点面 1 文件与零复制面 0 命中是同一次检索在 §6.3 定义的两组互斥文件集上的互补结果，且 §6.4 追加了「断言引用常量符号、不内联锚短语」以保持落点面为 1 文件。其余可测试面复核未变：§4.4 三条行为 AC 的观察面复用既有会话记录、注入时刻早于任何工具调用；§4.5 的复跑入口为既有命令（output-guard 的 node --test output-guard/test/*.test.mjs 与 dep-14 声明的 CI 步骤）；§4.6 的 AC-12 取证算法路线无关且清单不可枚举即硬失败；§6.4 的测试改动全为增项，本轮实读 runtime_config_test.go:2135 TestBriefSkillsListIsNamesOnly 与 execenv_test.go:1517 TestInjectRuntimeConfigNoSkills 仍在，新增文本追加在段末不破坏两条断言。AC-5 行 ① 的断言面另有非阻塞提醒，见 suggestions。"
  Prompt采纳影响: "条件未触发，N/A 成立（本轮复核）——本 CR 的 diff 不触及 skills/shared/crctl/scripts/crctl.mjs 的 dispatch 分支，也不触及 skills/shared/controlled-shell/rules.json 的 protectedPaths.deny（单元 A 在 multica daemon 的文本合成、单元 B 在 Pi 版本化源码），故 §8 不写采纳清单；§6.4 的零改动清单已覆盖 dep-10 的 crctl 与 guard 声明面。"
suggestions:
  - "已解决：B-1（上一轮唯一 blocker，canonical review-annotations/sdd.yml 的 blockers[0]，原文以「本轮新增：位置：§3.2「零复制面（AC-1 的取证对象）」、§6.2 的 AC-1 行、§6.3 的 dep-13（relative path 与依赖结论）、§9 zero_diff 的 Z-5 行；对照面为 §6.4 零改动核对清单的同一项」开头）——本轮逐处复核，五处已按同一 carve-out 收口：① §6.3 的 dep-13 把 relative path 收窄为「cr-prompts-revised/** 与 server/internal/** 中本 CR 落点之外的文件（落点＝dep-4 的 runtime_config_sections.go 与 dep-8 中改动的 runtime_config_test.go）」，依赖结论补 carve-out 的必然性并声明与 §6.4 核对清单、§9 Z-5 使用同一限定表述（唯一裁决）；② §3.2 的零复制面子句加同一限定，并写明「落点面命中 1／零复制面命中 0」是同一次检索的互补两侧、不是两个独立判据；③ §6.2 的 AC-1 行改为「落点面命中数 = 1，即命中集 = {runtime_config_sections.go}」与「零复制面（落点之外）0 命中」，并把零复制面文件级写为「除 runtime_config_sections.go／runtime_config_test.go 外的文件」；④ §9 Z-5 的对象列明确列为 dep-10 + dep-13，并注明「本项不含 dep-4／dep-8 的落点文件——后者由 scope_in 第 1～2 项要求修改（判据 ①：同一对象不得同时被要求修改与不修改）」；⑤ §6.4 的零改动核对清单与 §4.3 第 4 条同步同一限定。事实面本轮实测：检索锚短语在 multica worktree 与 tools worktree（排除 .git 与 node_modules）命中数均为 0，落点两文件（runtime_config_sections.go、runtime_config_test.go）在 multica@59b47993 上均存在，server/internal 实测 1479 文件、扣掉落点两文件即 1477；KB 侧仅 sdd.md 自身与评审记录含该短语（属契约文档而非复制面，AC-1 的检索面由 SDD 定义为 dep-13 与 dep-10 两侧）。§3.2 的 0 命中与 §6.2 的 1 文件命中集不再互斥，Z-5 不再对 scope_in 要求修改的同一对象声明不得改动。结论：B-1 关闭。"
  - "已解决：上一轮 suggestions S-1～S-5 逐条复核通过——S-1 §6.3 末尾索引块已改为「dep-3/4/5/6/7/8/9/13 multica」与「dep-10/12/14 tools」（本轮实测 14 条 dep 的 40 位 SHA 与三仓 HEAD 全等，multica 行不再挂 dep-13 之外的 tools 编号）；S-2 采纳第二方案，§4.2 的信号值改为 timeout === undefined ? DEFAULT_TIMEOUT_MS / 1000 : timeout，显式路径的信号值即入参本身、模板求值与现状逐字相同（NFR-1 在全值域字面成立，含往返不精确的双精度值），DEC-4 与 SDD-CLOSE-02 同步；S-3 §7.3 已改为实读值（本轮实测 §3.2 逐字文本 = 108 个空白分隔词、107 个含字母词）；S-4 §6.5 新增「DEC-2 后果的取证归属」段，写明 PowerShell 未传路径的取证归入 AC-5 的同一判据、dev-plan／TASK 不另造验收面，F-1 出口保留；S-5 §6.5 与 §9 F-2 写明 Pi 源码仓的建仓／源码检出属本 CR dev-plan 的决策动作、F-2 只承载 repositories 声明变更，判据 ④ 不再被字面命中。"
  - "本轮新增：S-6（§6.2 的 AC-5 行 ① 与 §6.4 单元 B 用例 ① 的断言面，非阻塞）——两处把断言写成 resolveTimeoutMs(undefined) === 300_000，但该符号在当前实现中是模块私有：本轮实测 dist/core/tools/bash.js 只在 :14 定义、:38 一处调用，文件的 export 面仅为 bashToolSystemPromptContribution／createLocalShellOperations／createLocalBashOperations／createShellToolDefinition／createBashToolDefinition／createBashTool，bash.d.ts 中 resolveTimeoutMs 与 DEFAULT_TIMEOUT_MS 命中数为 0，故该仓既有测试面无法直接 import 该函数。影响：该断言按字面不可执行，实施期需在「从工具入口取证（createShellToolDefinition + 假时钟的未传 timeout 用例，与 §6.4 用例 ② 同路径）」与「显式导出该函数（会改动模块公开面，需同步 .d.ts 构建产物）」之间选定一种并写进 TASK；不影响 PRD AC-5 的可达性（未传即 300 秒默认值与 schema 文案一致仍可由工具行为取证），也不改变本阶段结论与 passCondition。若选定路线的源码仓导出面与此不同，以该仓实测为准。"
review-loop:
  current-attempt: 2
subject-file: change-requests/CR-2026-070/sdd.md
subject-sha256: 00a3f7274b1081f87776b7e68c8ce6a6be2b3f899a87bae23fe2699952536e4d
