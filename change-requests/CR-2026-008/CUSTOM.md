# CUSTOM — CR-2026-008 裁量决策留痕

> 记录本 CR 起草过程中参考文档未明说、由需求 owner 裁定的决策，供评审与后续 CR 追溯。

## DEC-1 Private Ask 会话模型：单活跃会话（get-or-create）

- **决策**（2026-08-02，Ray）：每项目每人一个 Private Ask 会话，进入 tab 时按
  `(project_id, creator_id)` get-or-create（取该键下最新活跃会话，无则建）。
  本 CR 不做会话列表/多会话切换/清空上下文。
- **依据**：切分文档 CR-C 节只定义"按 `(project_id, creator_id)` 查询会话"，量级 2–3 人日；
  会话列表 UI 纳入即超量级。清空上下文本就被切分文档 §0.4 排除（依赖 daemon usage 回调）。
- **后路**：B2 的 `(project_id, creator_id)` 索引天然支持将来放开为"列出多会话"，表结构不堵死。
- **落点**：prd.md FR-2 + §7 排除项；SDD 阶段需写明 get-or-create 取法避免实现歧义。

## DEC-2 输入区能力：以 §0.4 为准剔除斜杠命令

- **决策**（2026-08-02，Ray）：Private Ask 输入区按设计稿 §6 该列取齐
  （附件/技能/模型选择器 ✓、@仅成员、固定 Ask、发送/停止），但该列的
  "斜杠命令（只读类）"剔除，不随本 CR 交付。
- **依据**：切分文档 §0.4 是"写死排除，防后续被当遗漏"，效力高于设计稿 §6 能力矩阵；
  设计稿 §6.1 亦标注斜杠命令为 2026-07-29 独立增补（S4），本就应独立交付。
- **说明**：设计稿 §6 与切分文档 §0.4 的冲突是文档间既有事实，非本 CR 引入，不回改文档。
- **落点**：prd.md FR-8 + §7 排除项。
