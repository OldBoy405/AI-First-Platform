# CR-2026-027 实施记录

## TASK-01 bootstrap（2026-08-10）

- workspace `dir-graph.yaml#repositories` 已声明 `tools`（path `../tools`、trunk `custom/main`、role `code`、active `true`）——提交 `b3dcd9f`
- `../tools` 仓 worktree 已派生：`.rayai-worktrees/tools/requirement/CR-2026-027`（分支 `requirement/CR-2026-027`）
- **bootstrap-base-sha** = `ad5b6a29bd70017fc5a160a36b1d1aa414081da9`（派生时 custom/main HEAD，AC-22 断言依据）
- workspace `AGENTS.md` 状态机口径 25/47 → 27/49（含 CR-2026-022/026 四次转换历史）
- v2 方案文档 §2.2 删除旧版 command module 描述（保留"不创建 commands/ 模块目录"决策）

后续 TASK 记录追加于本节。

## TASK-05 幽灵条目清理（2026-08-10）

- `crctl migrate-backlog` 新增幽灵块清理阶段（重复 key 检测 + 遍历所有条目块 + history 行级 title 归属判定 + 幂等）——tools worktree 提交 `bd0ebea`/`53fbab2`
- knowledge-base worktree 执行清理：幽灵块（CR-2026-024 残留）删除，CR-2026-017 条目字段恢复完整——提交 `e3fb890`
- 幂等验证：再次运行返回 `already-clean`

## 代码评审二轮回修 b10（2026-08-10）

- **b10a FR-10/一致性**：幽灵清理审计事件从 `migrateGhostCleanup` 内预写移出——先 `casWrite` 成功、后由调用方 `auditGhostCleanup` 补记；CAS_CONFLICT 时 `_backlog.yml` 不变且 audit.log 零成功记录——tools worktree 提交 `3d4be47`
- **b10b 开发启动门禁**：新增受控历史审批迁移 `crctl approve <cr> --stage <stage> --resign <reason>`（TTY 人类在环、无旁路）：gates.json evidence 定义变更（dev-start 剔除 task-index）导致既有 approval digest 漂移时，按当前定义重算并只改写该段（保留 approver/approved-at/via/target-status），追加 resign 审计子块与 audit 事件，幂等——tools worktree 提交 `3d4be47`
- 重测 146/146（143 既有 + 3 个 b10 向量），`lint-prompts enforce` 0 findings——knowledge-base worktree 提交 `5b22192`（PRD/SDD/test-report 同步）+ `829b926`（traceability tests 段）
- 待人工执行：`crctl approve CR-2026-027 --stage dev-start --resign "evidence-definition-change（gates.json dev-start evidence 剔除 task-index）"` 迁移审批 digest（仅限交互式终端），随后重新进入 review-code
