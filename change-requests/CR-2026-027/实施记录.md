# CR-2026-027 实施记录

## TASK-01 bootstrap（2026-08-10）

- workspace `dir-graph.yaml#repositories` 已声明 `tools`（path `../tools`、trunk `custom/main`、role `code`、active `true`）——提交 `b3dcd9f`
- `../tools` 仓 worktree 已派生：`.rayai-worktrees/tools/requirement/CR-2026-027`（分支 `requirement/CR-2026-027`）
- **bootstrap-base-sha** = `ad5b6a29bd70017fc5a160a36b1d1aa414081da9`（派生时 custom/main HEAD，AC-22 断言依据）
- workspace `AGENTS.md` 状态机口径 25/47 → 27/49（含 CR-2026-022/026 四次转换历史）
- v2 方案文档 §2.2 删除旧版 command module 描述（保留"不创建 commands/ 模块目录"决策）

后续 TASK 记录追加于本节。

## TASK-05 幽灵条目清理（2026-08-10）

- `crctl migrate-backlog` 新增幽灵块清理阶段（重复 key 检测 + 遍历所有条目块 + history 行级 title 归属判定 + 幂等）——tools worktree 提交 `bd0ebea`/`53fbab2`
- knowledge-base worktree 执行清理：幽灵块（CR-2026-024 残留）删除，CR-2026-017 条目字段恢复完整——提交 `e3fb890`
- 幂等验证：再次运行返回 `already-clean`
