cr-id: CR-2026-065
review-type: dev-plan
reviewer: "OldBoy405"
reviewed-at: "2026-09-13T11:19:26+08:00"
verdict: block
repair-target: write-tech-design
blockers:
  - "本轮新增：已批准 SDD §3.4/§4.2 的 TAP「文件名块 + file 级 plan」per-file 归属前提在目标运行时为假 —— 验收面断裂、不可达（位置：sdd.md §3.4 解析规则 / §4.2 主干 manifestCheck / §2.3 manifest.files+manifest.cases 语义 / TDEC-1；阻断事实与出口已在 plan.md §0.0、§6.1 表注⑤、§7 阻断标注、§9.1 与 tasks/TASK-03.md §0+§3.4、tasks/TASK-04.md §0 登记，本节不经任何自报转述）。事实（本轮 reviewer 第一手复核）：(a) 自建两文件探针（a.test.mjs 两用例 + b.test.mjs 一用例）执行 node --test --test-reporter=tap，在 Node v24.15.0（本机，SDD §6.3 登记环境）与 v20.20.2（CI：.github/workflows/crctl-ci.yml:46-48 node-version 20）上均得扁平 TAP：# Subtest: 三行全为用例名、以 .test.mjs 结尾的 # Subtest: = 0、全文唯一 plan = 全局 1..3、passing 用例块内无任何文件归属信息；(b) 显式文件列表、目录发现（node --test）、--test-isolation=none 三种形态结论一致；(c) 文件名块唯一出现的形态是整文件加载失败（# Subtest: c.test.mjs + not ok 1 - c.test.mjs），即 SDD §3.2 表 SUITE_FILE_LOAD_FAILURE 的错误形态，不是正常文件的正常分组。影响：files_executed、manifest.files 集合核对、manifest.cases per-file 基线三项在真实输入下不可判（按 §3.4 字面实现则 files_executed 恒为 ∅、以 SUITE_REPORT_UNPARSEABLE 硬失败收场），cmd-01 永红 ⇒ AC-01/AC-10/AC-11/AC-12/AC-13/AC-14 在本计划下不可达、FR-11…FR-17 的验收证据面断裂；根因是 SDD 自身的机制设计在目标运行时不存在，不是 plan/TASK 把批准范围译错（plan/TASK 的覆盖表、矩阵、接口契约、依赖拓扑、组映射、zero_diff 面、粒度本轮未发现独立缺陷）。修复方向：repair-target=write-tech-design（route=upstream）—— 由人工走「修订 SDD §3.4/§4.2 的 per-file 归属机制（例如改用携带文件归属的报告形态、逐文件执行后聚合、或把 manifest 语义改为目标运行时下可实现的等价口径）→ 重新技术评审 → 重新人工审批 → replay 本计划」链；plan/TASK 不得就地放宽（加第二 reporter 目的地 / 逐文件 spawn / 降全局口径），也不得修改 sdd.md 一个字（本计划已按此办理，正确）。"
dimensions:
  sdd-to-plan: "pass：SDD §1.2 变更面 11 个路径与 §6.1/§6.2 设计落点全部进入 plan（§1 里程碑 M3…M6、§2 依赖图与共享契约、§4.0/§4.1、§6.1/§6.2/§6.3/§6.4/§6.5、§7 矩阵、§9 组映射）；SDD §6.6 D/P 两表、§2.4 报告模型、§3.2 四查与 check code 集合被 plan §2 与 §6.5 J-1…J-8 承接；plan/TASK 对 sdd.md 零改动（本轮按 worktree 实际文件重算 sha256(LF) = 1e6af84f6645879c09180ec5e11048d94b1f1ff48fe2caa82b44a5b337fe12b4 / 68768 B / 行尾 CR 计数 0，与 review-annotations/sdd.yml#subject-sha256 全等）。本维度不重审已审批 SDD 决策；SDD 设计前提为假一事由上条 blocker 承载，不在此重复计缺陷。"
  plan-to-tasks: "pass：plan §6.1 17 行（FR-1…FR-17 各恰一行、每行恰一个主责 TASK、验收证据列全为两位十进制 cmd-NN）与 §7 18 行（AC-01…AC-15 + 3 条业务闭环、每行恰一个 TASK owner、证据均可回指 cmd-NN）本轮机械复核通过；tasks/_index.yml 的 id 集 ≡ §6.1 主责集 ≡ §7 owner 集 ≡ 四张 TASK 卡 frontmatter id 集（双向，无表内引用不到账本、无账本存在而表内不引用）；_index.yml 与四卡 frontmatter 的 id/title/estimate/depends-on 逐项全等；估算 16+16+20+16 = 68h 与 plan §1/§5.4/§9 同口径；§9 组映射 G1…G4 与卡内 depends-on、§2 依赖图一致。"
  task-executability: "pass：四卡 6 节齐备（任务描述 / 涉及文件 / 实现要点 / 验收条件 / 完成标志 / 接口契约），各含 4 条可执行验收（高于 ≥2 下限）；TBD / 空泛指令 0 命中（TASK-03 唯一 TODO 命中是 TAP「# TODO」后缀语义，非待办项）；target-version 0.38 继承自 cr.md（非 tbd）；TASK-03 §0 与 TASK-04 §0 的阻断声明把「不可完成的事实 + 已落地可复用部分 + 出口」写清，未含任何降级或放宽；四卡完成标志全部落在 developing 内可被 crctl task done 登记的事件（实现落盘 + 证据命令绿 + 账本登记），无 merge / writeback / archive / code-reviewing / code-approved 前置。"
  dependency-topology: "pass：01/02 → 03 → 04 为 DAG（TASK-01/02 无依赖、TASK-03 依赖 01+02、TASK-04 依赖 01+02+03），无悬空引用、无环（本轮由图遍历复核）；产出/消费顺序与 §9、各卡 §6 声明一致：TASK-03 消费 gate-registry.json 与 assertion-sources.mjs、lib/outbox-contract.mjs；TASK-04 消费 suite-gate.mjs CLI、13 字段报告模型与并发常量；TASK-01 与 TASK-02 互不依赖、可并行。"
  interface-contracts: "pass：四类共享契约在 plan §2 / TASK-01 §6 / TASK-02 §6 / TASK-03 §6 / TASK-04 §6 五处同写法 —— (1) deriveStateMachine(toolsRoot) 返回 { namedStates, wildcards, transitions, identifiers, expandedCount, declaredCount } 且推导失败 throw（禁返回空集合）；(2) lib/outbox-contract.mjs 五导出（OUTBOX_COMPARED_FIELDS / OUTBOX_EXCLUDED_FIELDS = [occurred_at] / OUTBOX_VOLATILE_PAYLOAD_KEYS = [detected_at]（payload 根下相对键）/ buildOutboxEvent / buildOutboxComparable）与 SDD §3.1 一致；(3) gate-registry.json schema crctl-suite-gate/v1 四段（manifest.files / manifest.cases / stateMachine.* / exceptions）与 SDD §2.3 一致；(4) suite-gate.mjs 两形态 CLI 与 13 字段报告模型（TASK-03 §3.3）字段集合与 SDD §2.4 一致。TASK 新事实按 resources[].worktreePath（tools worktree，HEAD 2c84241、clean）抽验：.github/workflows/crctl-ci.yml:111 现文 = node --test --test-concurrency=2 skills/shared/crctl/scripts/test/*.test.mjs、:46-48 node-version 20，与本轮未改该文件的事实相符。"
  acceptance-verifiability: "block：两张稳定表结构完整且机械闭合（§6.1 17 行 + §6.2 五条 cmd-01…cmd-05 双向唯一映射；本轮复核无未定义引用、无未被引用的证据 id），每个关键 AC 恰一个唯一 TASK owner，cmd-04 的 diff 白名单判据本轮实测有效（tools worktree 相对 dddd0ad6 的 9 个改动路径全部落在白名单内）。但 cmd-01 的 per-file 归属前提不存在（见 blocker）⇒ FR-11…FR-17 的接受面不可达、AC-01/AC-10…AC-14 不可达；属 SDD 设计面缺陷（repair-target=write-tech-design），不是 plan/TASK 的翻译错误。"
  scope-and-simplicity: "pass：改动面 = SDD §1.2 的 11 个路径（含 4 个新增文件）；scope_out 零触碰（包 B、CR-2026-064 恢复合同迁移、5 条红根因对象本体、新依赖 / 新框架、历史证据、multica 仓）；§9 follow_up 三处硬编码断言不进本轮 diff；zero_diff 面逐卡声明并由 cmd-04 的 diff 白名单机器兜底；无新增 crctl 子命令 / flag，无新增测试文件（*.test.mjs 保持 21），无新增第三方依赖；拆分粒度 2 / 2 / 2.5 / 2 天落在 1-3 天区间。"
  risk-and-rollback: "pass：RU1…RU4 与四卡一一对应，组合回滚顺序显式（RU4 → RU3 → RU2 → RU1 逆拓扑）；R-04 已由「中风险」升级为「阻断（已实现）」并登记排除性核查与「不得就地放宽」的出口；R-01/R-02（证据集预算 ≈930 s vs write-test-report 20 min、--max-runtime-ms 1200 s < cmd 超时 1500 s 使停滞以门禁自身字段落证据）、R-10（manifest.cases 初值只可能偏低 + 终值 ≥ 初值）、R-11（实施期 10 次整跑排期）口径一致；三类注入的注入物由还原后 status 留痕 + cmd-04 白名单双重兜底，且 AC-11 明确不接受删减任何一次注入 / 还原。"
  reviewer-model: "deepseek-flash (deepseek provider, pi runner, reasoning=max)"
suggestions:
  - "范围外：（attempt 1 遗留，本轮仍未处理）plan §2 共享契约行与 §6.1 FR-14 行的「check code 11 个」与 SDD §3.2 表实际枚举的 13 个 check code 不符（本轮复核：SDD §3.2 去重后恰 13 个 SUITE_* / EXCEPTION_* code，TASK-03 §3.5 已完整复述 13 个且集合全等）。归属节点：write-dev-plan（plan 文本计数；实现唯一性与验收可达性不受影响）。修复方向：两处计数改为 13（或写「10 行 / 13 个 code」）。"
  - "范围外：（attempt 1 遗留，本轮仍未处理）TASK-03 §4.1 的窄跑验收（--test-name-pattern CR-2026-065）存在空跑即绿的口子 —— Node 的 --test-name-pattern 无命中时不报错、退出码为 0，且未命中的测试文件仍作为文件级用例计一次通过；TASK-03 §3.6 未像 TASK-02 §3.1 那样写明新增用例名必须以 CR-2026-065 起始。归属节点：write-dev-tasks（该 TASK 的真实证据链由 cmd-01 承载，故不阻断）。修复方向：§3.6 补用例命名约束，或 §4.1 要求附被命中用例名清单。"
  - "范围外：（attempt 1 遗留，本轮仍未处理）负控证据 JSON 的固定字段与 cmd-05 的读取面不一致 —— plan §6.4 与 TASK-04 §3.2 列的固定字段是 command / duration_ms / converged / exit_code / failures[] / injection / restore，而 cmd-05 要求每个 NC-*.json 顶层含 verdict 且取 block（注入）/ pass（还原）。归属节点：write-dev-tasks（TASK-04 §3.2）。修复方向：把 verdict 补进固定字段，避免按 §3.2 字面产出证据时 cmd-05 直接红。"
  - "范围外：（attempt 1 遗留，本轮仍未处理）cmd-01 未带 --json-out，而 TASK-03 §4.2 的验收写作「报告（--json-out）字段齐备」—— §6.2 的 cmd-01 args 受逐字转录纪律约束，故 §3.3 的全部字段必须由 --run 打到 stdout 才能进入 test-evidence/cmd-01.log。归属节点：write-dev-tasks（TASK-03 §3.3/§4.2）。修复方向：§3.3 明写 --run 以 JSON 输出全字段到 stdout（J-8 禁词约束不变），§4.2 措辞改为「cmd-01 日志 / stdout」。"
  - "范围外：（attempt 1 遗留，本轮仍未处理）plan §1 的 tasks/_index.yml#totalEstimateHours 指向账本里不存在的字段（_index.yml 只有 cr-id 与 tasks[]，totalEstimateHours 是 crctl task init 的返回值）。归属节点：write-dev-plan；不影响 68h 数值本身（16+16+20+16 与四卡 frontmatter、账本 estimate 三处全等，本轮复核通过）。"
  - "范围外：（本轮新增）plan 内「阻断 AC 清单」三处不一致：§0.0 列 5 条（AC-01/AC-11/AC-12/AC-13/AC-14，缺 AC-10）、§7 阻断标注列 6 条（含 AC-10）、§6.1 表注⑤ 另称「FR-11…FR-17 验收证据面断裂」；其中 AC-13/FR-15 的唯一证据是 cmd-04（diff 白名单 + 登记面 schema），其不可达是 TASK-03 阻断的传递结果而非 cmd-01 直接断裂。归属节点：write-dev-plan；不影响 route（upstream 不变）。修复方向：统一为单一清单并区分「直接断裂（cmd-01 依赖）」与「传递阻断（依赖被阻断的 TASK）」。"
  - "范围外：（本轮新增）plan §0 路径权威表的 tools 行 HEAD 仍写 dddd0ad63fb79bd7608314b4553f30e8ce7b7289（clean），而同一节 A1 行已记录本 CR 实施提交 2c84241，且本轮实测 tools worktree HEAD = 2c84241、status 为空 —— 表内自相矛盾（该值作为 cmd-04 的 diff 基线仍然正确）。归属节点：write-dev-plan。修复方向：该行区分「登记基线 dddd0ad6」与「当前 HEAD」。"
  - "范围外：（本轮新增）tasks/TASK-01.md 与 tasks/TASK-02.md 的 frontmatter status 仍为 pending，而 tasks/_index.yml 已登记 done + done-at（本轮实测两者不一致）；账本为权威（crctl 只读 _index.yml 的任务状态，完成登记已即时落账，符合工程纪律 #8），卡内 status 不参与任何门禁。归属节点：write-dev-tasks。修复方向：replay 时同步卡片 frontmatter status，或在 plan/卡内声明卡片 status 非权威。"
review-loop:
  current-attempt: 1
subject-sha256: ad9343d4c3bc23b65c12668aa020f2e1a546d2d2ddf8787fad67ba2f13610e99
