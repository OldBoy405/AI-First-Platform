# CR-2026-047 测试映射清单（test-mapping.md）

> TASK-11 收口产物：AC-1~AC-22 → 可执行测试用例的逐条映射。
> 全部 Go 测试在真实 PostgreSQL（本机 `postgres://multica:multica@localhost:5432/multica`）与
> 无 DB 的纯函数层分别执行；前端为 vitest。生成器为 `node --test`。

| AC | 测试用例 | 位置 | 状态 |
|---|---|---|---|
| AC-1 配置校验 | `TestValidateConfig`（缺 key/未知 key/权重和≠1/target≤floor/weeks/status 全枚举）+ 生成器硬校验（`generate-config.test.mjs` weight sum/缺块/unrecognized line） | `server/internal/maturity/schema_test.go`、`server/internal/maturity/gen/generate-config.test.mjs` | ✅ |
| AC-2 生成器 --check / 漂移 | `generates gofmt-clean output and --check agrees`、`--check fails on drifted committed file`、`LF and CRLF sources produce byte-identical output`、`dirty source refuses generation` | `server/internal/maturity/gen/generate-config.test.mjs` | ✅ |
| AC-3 迁移 375–379 | 真实 PG `go run ./cmd/migrate up`/`down`/`up` 已执行（379→375 逆序回滚成功；`down` 全程在 373 中止为上游既有 CHECK 冲突，与本 CR 无关）；`TestConcurrentIndexCleanupsMatchTheirMigrations`、`TestEveryConcurrentUpBuildHasCleanup`、`TestEveryConcurrentDownBuildHasCleanup`、migration lint（`internal/migrations`）；EXPLAIN 命中 378/379 由 `TestMaturityIndexesServeTheirQueries` 钉死 | `server/cmd/migrate/migrate_mul5999_index_retry_test.go`、`server/internal/migrations/migrations_lint_test.go`、`server/internal/service/maturity_index_explain_test.go` | ✅ |
| AC-4 调度 fixed-clock 前一自然日 | `TestMaturityPlansForScopeFirstStart`（00:15→前日 00:30、00:31→当日 00:30 均恰好一条）、`TestMaturitySnapshotHandlerBucketDate`（00:30→前一日）、rollup 集成测试验证 org/user/project 三 scope 单事务，并钉死“旧 task 在目标日产生 usage 时仍创建 user snapshot 且保留 30 tokens” | `server/internal/scheduler/jobs_maturity_test.go`、`server/internal/service/maturity_rollup_db_test.go` | ✅ |
| AC-5 幂等/并发/回滚 | `TestRollupMaturityWorkspaceIntegration`：按目标 bucket 精确 no-op、内容字节不变、advisory lock 持锁；`TestRollupMaturityWorkspaceFillsOlderMissingBucket` 验证较新 bucket 成功后仍补旧洞；JSON 校验失败整事务回滚 | `server/internal/service/maturity_rollup_db_test.go`、`maturity_rollup.go` | ✅ |
| AC-6 观察期 scores 空 + config 断点 | `TestRollupMaturityWorkspaceIntegration` 断言 observing seed 下 `scores={}`；`TestMaturityServiceReadPath` 断言 `total_score=null` 且 raw 正常 | `server/internal/service/maturity_rollup_db_test.go`、`maturity_test.go` | ✅ |
| AC-7 hook 补偿 | `TestMaturityPlansForScopeFirstStart/CatchUp/RetryMerge`：首启仅最近 plan、停机 3 天补 3 plan、超 7 天截 7、较老 FAILED+较新 SUCCESS 不搁浅 | `server/internal/scheduler/jobs_maturity_test.go` | ✅ |
| AC-8 首屏/成本四态 | `TestCostFromTokenRows`（authoritative 精确 1e-7、无价目 uncosted→unavailable）、`TestModelPrice`（provider/model 优先、bare fallback）；UI schema + 组件测试 cost 文案四态 | `server/internal/service/maturity_rollup_test.go`、`packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-9 趋势三维 + 隐私 | `TestMaturityServiceReadPath` 验证 latest 与 project 趋势；model SQL 按 Asia/Shanghai 日分桶；`TestMaturityHandlerGuards/token-trend foreign user rejected`（400 + unsupported_user_dimension）；UI 覆盖 project/self/model 三模式、日期范围与 config revision 断点 | `server/internal/service/maturity_test.go`、`server/pkg/db/queries/maturity.sql`、`server/internal/handler/maturity_test.go`、`packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-10 无个人榜 | `TestMaturityHandlerGuards/rankings scope=user rejected`（400 unsupported_scope）；UI 测试断言无 user ranking 入口 | `server/internal/handler/maturity_test.go`、`packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-11 观察期无雷达 | `renders observing banner and no radar`（queryByTestId maturity-radar 为 null） | `packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-12 8 公式 + owner unresolved | `TestMetricScore`（floor/target/夹断/NaN）、`TestDimensionScores`/`TestTotalScore`（不可用项拒绝重归一化）、`TestMaturityServiceReadPath`（free-text owner → collab unavailable + reason 精确 + scores 空） | `server/internal/maturity/score_test.go`、`server/internal/service/maturity_test.go` | ✅ |
| AC-13 治理三态不进总分 | `TestValidateSnapshotMetrics`（6 键恒在）、`TestMaturityServiceReadPath`（governance 6 字段、trace unavailable）；计分函数不读 governance（`BuildScores` 只取 metric_values） | `server/internal/maturity/schema_test.go`、`score.go`、`server/internal/service/maturity_test.go` | ✅ |
| AC-14 数量与治理同屏/定义页 | 组件测试断言 governance、Token/质量配对、8 项 known gameability、definitions、Owner mode 与 date range 控件渲染；production tree 直接复用 `UsageTrendCard`（内含 `DimSegmented`）和 `Leaderboard` 的通用变体，全部 typography 使用语义 role token、内部导航使用 `AppLink` | `packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-15 六端点契约 | `TestMaturityServiceReadPath`（overall/rankings/trend/config/suggestions/history 全走一遍；损坏 metrics/scores JSON 投影 fail-closed；未知 ranking metric/坏 cursor 显式 invalid query）+ `TestMaturityHandlerGuards`（未知 metric 与 report cursor 均 400）+ zod 验 snake_case wire→camelCase domain（含嵌套 attribution 与 Org Admin IDs）及 malformed fallback | `server/internal/service/maturity_test.go`、`server/internal/handler/maturity_test.go`、`packages/core/api/maturity-schemas.test.ts` | ✅ |
| AC-16 Web/Desktop 路由 | web `apps/web/app/[workspaceSlug]/(dashboard)/maturity/page.tsx` + desktop `routes.tsx` 条目；`paths` 一致性测试（`consistency.test.ts`/`route-icons.test.ts`）全绿 | 路由文件 + `packages/core/paths/*` 测试 | ✅ |
| AC-17 Org Admin 幂等 | `TestEnsureOrgAdminWorkspaceIdempotent`（两次调用同四个 ID；project settings system_key / agent system_key=`org-admin` / autopilot / schedule trigger / active rule version 各唯一一行，project lead 指向 Agent）；建议空态组件验证 Owner/Admin 可选在线 runtime 并调用 `ensureOrgAdminWorkspace` | `server/internal/service/org_admin_test.go` | ✅ |
| AC-18 周报同周幂等 | `TestEnsureOrgAdminWorkspaceIdempotent` 真实 PG 先验证缺 `local_directory` 时 run=skipped、无 task、Owner 收到 action_required 指引；绑定目录后覆盖 schedule dispatch→project/chat task→无效 envelope fail-closed→direct result/chat/inbox；第二个同周 retry task 复用 chat，事务 advisory lock 保证 inbox 仍为 1；另验证同 Agent/项目的非 canonical Autopilot 不被误分类；Skill 声明 Write/mkdir/sha256sum/mv 并规定同目录 temp→rename，实际 daemon 落盘见残余风险 R-2 | `server/internal/service/org_admin_test.go`、`maturity_completion.go` | ✅（server 全链路）/ ⏳（真实 daemon 文件） |
| AC-19 报告五节 | `TestMaturityReportBuiltinSkillContract` 同时断言五节标题、指标 key 与可执行文件工具权限；completion validator 缺节即拒绝完成 | `server/internal/service/builtin_skills/multica-maturity-weekly-report/SKILL.md`、`org_admin_test.go`、`maturity_completion.go` | ✅ |
| AC-20 建议最新/历史 | `TestEnsureOrgAdminWorkspaceIdempotent` 生成 W34/W35 报告并把 W35 `completed_at` 人为回拨，仍断言 latest=W35；history limit=1 两页按 immutable `report_key` keyset 顺序返回 W35→W34，插入新完成项不会造成 offset 跳项；`decodeReport` 验 schema+SHA，坏 cursor=400；zod 覆盖空态 | `server/internal/service/maturity_test.go`、`server/pkg/db/queries/maturity.sql`、`packages/core/api/maturity-schemas.test.ts` | ✅ |
| AC-21 追问深链 | production dispatch 按 project+Owner 复用 chat；同周第二 task 断言 `chat_session_id` 相同；`TestPersistMaturityReportCompletionIgnoresFollowUpTurns` 保证普通追问不被报告校验拦截；UI href 携 `?session=` | `server/internal/service/org_admin_test.go`、`packages/views/dashboard/maturity/maturity-page.test.tsx` | ✅ |
| AC-22 基线 P10/P75 不写 config | `TestMaturityConfigExposesTwentyEightDayBaseline` 在真实 PG 写 28 个连续 org bucket，断言全部 8 项 sample_count=28 且 P10<P75；生成器 `--check` + dirty guard 保证配置不被运行时写 | `server/internal/service/maturity_baseline_db_test.go`、`server/pkg/db/queries/maturity.sql`、`generate-config.test.mjs` | ✅ |

## 残余风险

- **R-1 真实 PG 迁移 down 全量回滚**：379→375 逆序回滚已在真实 PG 验证；全库 `down` 在 373 因上游存量数据违反其 CHECK 中止，与本 CR 无关。
- **R-2 真实 daemon 文件落盘**：server 端 dispatch/completion/chat/inbox 已真实 PG 贯通；内置 skill 的 temp-file + atomic rename 仍需在绑定 `local_directory` 的实际 daemon 环境做部署验收。
- **R-3 前端手工验收**：targeted vitest 18 files / 134 tests 与 typecheck 通过；尚未执行真实 Web/Desktop 视觉、响应式与键盘可达性走查。
- **R-4 全量 views 基线**：补充全量 suite 的 3 个失败均在 merge-base 至 HEAD 未改动的 project-detail mock 与 Windows rich-content 路径测试；CR 范围 targeted suite 全绿。
