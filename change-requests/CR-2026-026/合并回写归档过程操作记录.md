# CR-2026-026 合并·回写·归档 过程操作记录

> 时间：2026-08-09 14:45–15:25（Asia/Shanghai）
> 依据：`merge-feature-branch` / `writeback-prd-sdd` / `writeback-tasks` / `writeback-traceability` / `cr-archive` Skill
> 本文记录三阶段全部 40 次工具调用；合并（M1-M5）14 次、回写（W1-W3）18 次、归档（A1-A3）8 次

## 一、合并（merge-feature-branch，code-approved → merging）

| # | 工具调用内容 | 为什么调用 | 结果 | 归属逻辑步骤 |
|---|---|---|---|---|
| 1 | Bash `crctl status` + worktree `git status` | 确认人工审批结果与待提交文件 | ✅ status=code-approved（用户已执行 approve-code）；approval.yml 待提交 | M1 前置 |
| 2 | Bash `git add approval.yml` + commit（6cef9a9） | 落盘 code 审批记录 | ✅ 6cef9a9（approval 段：approver/approved-at/via/target-status） | M1 前置 |
| 3 | Read `merge-feature-branch/SKILL.md`（229 行） | 确认四阶段合并协议：预检/本地合并/远端发布/自动补偿 + 参与仓解析 | ✅ Step 1-6 明确；tools 仓先例（custom/main 直接提交）确认 | M1 前置 |
| 4 | Bash Get-Content dir-graph + Select-String 025 + `rev-parse` HEAD/origin + tools branch | 解析参与仓 + 找 025 merge-commits 先例 + 核对分支远端状态 | ⚠️ Select-String `-A` 参数错误（PowerShell 用 `-Context`）；origin/requirement/CR-2026-026 不存在（未推送） | M1 前置 |
| 5 | Bash Get-Content dir-graph + Select-String `-Context` 025 | 修正参数后解析 repositories | ✅ 2 个 active repo：ai-first-platform-docs(master) + multica(main)；_backlog 无 025（已归档） | M1 前置 |
| 6 | Bash Select-String _history 025 + Get-Content _backlog tail | 取 025/023 归档条目的 merge-commits 形态作先例 | ✅ 先例：docs 记录 merge sha + branch；tools 记录直接提交 sha（025=9a8abb5）或 none（023） | M1 前置 |
| 7 | Bash `git status`（主仓）+ push 分支 + `rev-parse` 核对 | Step 1.4：推送 knowledge-base CR 分支并核对本地=远端 | ✅ 分支推送成功（origin/requirement/CR-2026-026=6cef9a9）；主仓有无关脏文件（P3 文档 M + 记录文件 ??，不影响 merge） | M1 前置 |
| 8 | Bash fetch + `rev-parse` preflight + `merge-tree --write-tree` | Step 2 全仓预检（dry-run，不修改 trunk） | ✅ preflight-trunk=dbd47b8、source=6cef9a9；MERGE-TREE OK 无冲突 | M2 预检 |
| 9 | Bash checkout master + pull --ff-only + `merge --no-commit --no-ff` | Step 3 本地合并（两阶段：先 no-commit） | ✅ Automatic merge went well（P3 未跟踪文件不冲突） | M3 本地合并 |
| 10 | Bash `git commit` merge + `rev-parse` | 全部仓成功后提交 merge commit | ✅ 279232a（merge(CR-2026-026)） | M3 本地合并 |
| 11 | Bash fetch + rev-parse 复核 + push | Step 4 远端新鲜度复核（origin/master 仍=preflight）+ 统一 push | ✅ dbd47b8..279232a master -> master | M4 远端发布 |
| 12 | Bash `advance --to merging --embedded` + `merge-metadata` ×2（docs 279232a / tools d395d04） | Step 5 状态推进 + merge-commits 账本写入（唯一通道 crctl） | ✅ 执行成功（输出被过滤，后续核实） | M5 状态与 metadata |
| 13 | Bash status 确认 + Select-String backlog + git status | 核实状态与账本 | ✅ status=merging；merge-commits 两条（docs+tools 均带 branch） | M5 状态与 metadata |
| 14 | Bash Select-String merge-commits 全文 + `git add/commit/push`（7614e34） | 核对完整形态后发布 metadata | ✅ 7614e34（[cr] merge metadata，已 push） | M5 状态与 metadata |

## 二、回写（writeback，merging → writing-back）

| # | 工具调用内容 | 为什么调用 | 结果 | 归属逻辑步骤 |
|---|---|---|---|---|
| 15 | Read `writeback-prd-sdd/SKILL.md`（102 行） | 确认回写协议：脚本化（writeback-prd-sdd.mjs）、dry-run → 实跑 → 提交 | ✅ 参数（cr/spec/version/milestone-name）+ 幂等判据明确 | W1 PRD/SDD |
| 16 | Bash Get-ChildItem specs + Get-Content `_index.yml` | 确定 spec_id 与当前版本 | ✅ spec_id=ai-first-platform；current=0.26 → 本次 0.27 | W1 PRD/SDD |
| 17 | Bash `writeback-prd-sdd.mjs --dry-run` | Step 2 核对增量 diff（里程碑节追加 + _index 更新） | ✅ PRD/SDD changed=true noop=false；_index current=0.27/cr-ref=026/cr-history 追加 | W1 PRD/SDD |
| 18 | Bash `writeback-prd-sdd.mjs`（实跑） | Step 3 实跑 + 自检（里程碑标题恰 1 次、无 CRLF） | ✅ written=true EXIT=0（PRD/SDD 增量回写） | W1 PRD/SDD |
| 19 | Bash `git add/commit`（a9db1fb）+ `advance --to writing-back --spec-id` + commit + push | Step 4 提交回写 + 推进状态（回写期 advance 必带 --spec-id） | ✅ a9db1fb（3 files +553）+ 215db3d（status=writing-back，已 push） | W1 PRD/SDD |
| 20 | Bash status 确认 + `node -e` 读 feature-writeback pipeline | 确认 writing-back + 后续节点顺序 | ✅ writing-back；节点链 merge→prd-sdd→tasks→traceability→cr-archive | W2 前置 |
| 21 | Read `writeback-tasks/SKILL.md`（93 行） | 确认任务回写协议（done 任务原子回写 + 索引重建） | ✅ 幂等判据（id 集合）+ 文件命名格式明确 | W2 前置 |
| 22 | Bash Get-Content `tasks/_index.yml` + Grep 失败 | 查任务状态 | ✅ **7 个 TASK 全 pending**（实现期漏标 done，纪律 #8 实坑）；Grep 命令 PowerShell 不可用 | W2 前置 |
| 23 | Grep crctl.mjs `cmdTaskDone\|editTaskDone` | 确认 task done 前置态约束 | ✅ 硬锁 `['developing']`（L1348）——writing-back 期无法补标 | W2 特例决策 |
| 24 | Grep crctl.mjs `checkDeliveryIndexComplete` | 确认归档门禁是否强制 | ✅ `doneIds.length===0` 直接放行（L588）——不标 done 也能归档但任务永不进 delivery | W2 特例决策 |
| 25 | AskUserQuestion（特例补标/跳过/放宽前置态） | 回写期补标被锁死，需用户拍板 | ✅ 用户选**特例补标**（沿用 dev-plan.yml 先例） | W2 特例决策 |
| 26 | SearchReplace `tasks/_index.yml`（7 条 status→done + done-at） | 按拍板补标（账本手改例外，commit 注明） | ✅ 7 条 done（2026-08-09T15:02:00+08:00） | W2 特例补标 |
| 27 | Bash `writeback-tasks.mjs --dry-run` | 核对待回写文件列表 | ✅ toWrite 7 项（TASK-0.27-CR-2026-026-01~07） | W2 回写 |
| 28 | Bash `writeback-tasks.mjs`（实跑）+ `git add/commit/push`（f7cd0bc） | 回写 + 索引全量重建 + 提交（含 _index.yml 补标） | ✅ f7cd0bc（9 files +440；7 项交付文件 + _index.yaml 重建，已 push） | W2 回写 |
| 29 | Read `writeback-traceability/SKILL.md`（124 行） | 确认追溯链回写协议（milestone 段草稿 + merge-commits 由脚本从账本提取） | ✅ fr-chain 结构 + 幂等判据明确 | W3 追溯 |
| 30 | Write `_scratch/cr026-milestone.yml`（137 行） | 起草 milestone 段草稿（24 条 FR 链：fr/title/sdd/tasks/code/evidence） | ✅ 草稿落盘（merge-commits 不手写，脚本提取） | W3 追溯 |
| 31 | Bash `writeback-traceability.mjs --dry-run` | 核对头部字段更新 + 本 CR 段追加（mergeCommits 从 _backlog 提取） | ✅ mergeCommits 2 条（ai-first-platform-docs@279232a、tools@d395d04） | W3 追溯 |
| 32 | Bash `writeback-traceability.mjs`（实跑）+ `git add/commit/push`（39a8a79） | 追加累积段（既有段逐字节保留自检）+ 提交 | ✅ 39a8a79（1 file +150；milestone 编码前质量门禁，已 push） | W3 追溯 |

## 三、归档（cr-archive，writing-back → archived）

| # | 工具调用内容 | 为什么调用 | 结果 | 归属逻辑步骤 |
|---|---|---|---|---|
| 33 | Bash Get-ChildItem 定位 cr-archive SKILL 路径 | 找归档协议文件 | ✅ `skills/cr/cr-archive/SKILL.md` | A1 前置 |
| 34 | Read `cr-archive/SKILL.md`（173 行） | 确认归档协议：emit 事件 → advance → archive-move → 发布 → 清理（含无改动仓规则） | ✅ Step 1-7 明确；Windows worktree 清理兜底链确认 | A1 前置 |
| 35 | Bash `inbox-emit archived` + `advance --to archived --embedded` + `archive-move` | Step 2-3：先 emit 事件（移出 backlog 前）→ 推进 archived → 原子移入 history | ⚠️ inbox-emit **BAD_ARGS**（payload 中文引号转义）；advance/archive-move 成功 | A1 账本移动 |
| 36 | Bash inbox-emit 重试（简化 payload）+ status + git status | 重试 emit + 核实归档结果 | ❌ emit `CR_STATUS_NOT_FOUND`（条目已移出 backlog，archived 态拒写——**归档事件丢失**，审计由 audit.log 承载）；archive-move 已成功（cr.md=_backlog/_history 均 M） | A1 账本移动 |
| 37 | Bash `git add`（4 账本 + CR 目录含 7 份过程记录）+ commit + push（bfdbd85） | Step 6 提交并推送归档记录（清理前必须先发布） | ✅ bfdbd85（[cr] archive CR-2026-026，已 push） | A2 发布 |
| 38 | Bash worktree remove ×2 + prune + `push --delete` KB + multica ls-remote 判断 | Step 7 清理：两仓 worktree（含无改动仓 multica）+ 远端分支（存在才删） | ✅ KB worktree removed+pruned；multica worktree removed+pruned；KB 远端分支 deleted；multica skipped-no-remote-branch | A3 清理 |
| 39 | Write `cleanup-report.yml`（10 行） | 记录清理结果（cleanup-status: done + 各仓明细） | ✅ 落盘 | A3 清理 |
| 40 | Bash `git add/commit/push`（cddd50d）+ status + Select-String history | 提交 cleanup 报告 + 终态核实 | ✅ cddd50d（cleanup done，已 push）；_history.yml 含 026 富化条目（title/summary/merge-commits）；status 报 CR 已归档（预期） | A3 清理 |

## 结果摘要

- **合并**：docs 279232a + tools d395d04（直接提交先例）；multica 无改动跳过；status=merging
- **回写**：PRD/SDD v0.27（a9db1fb）+ tasks 7 项（f7cd0bc）+ traceability 累积段（39a8a79）；status=writing-back
- **归档**：archive-move 移入 history（bfdbd85）+ worktree/分支清理 + cleanup done（cddd50d）；final-status=archived
- **特例**：tasks/_index.yml 回写期补标（用户拍板，commit 注明）；inbox-emit 归档事件丢失（payload 转义 + 时序错位）
- **提交链**：279232a → 7614e34 → a9db1fb → 215db3d → f7cd0bc → 39a8a79 → bfdbd85 → cddd50d（全部已 push origin/master）

## 逻辑步骤对照

- **合并**：M1 前置与分支推送（#1-7）→ M2 预检（#8）→ M3 本地合并提交（#9-10）→ M4 远端发布（#11）→ M5 状态与 metadata（#12-14）
- **回写**：W1 PRD/SDD 回写（#15-19）→ W2 任务回写（#20-28，含前置态死锁 → 特例补标拍板）→ W3 追溯链回写（#29-32）
- **归档**：A1 账本移动（#33-36）→ A2 发布（#37）→ A3 清理（#38-40）
