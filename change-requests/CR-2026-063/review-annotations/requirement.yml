cr-id: CR-2026-063
review-type: requirement
reviewer: "OldBoy405"
reviewed-at: "2026-09-11T18:01:52+08:00"
verdict: pass
blockers: []
dimensions:
  结构完整性: "通过：§1 概述（含 §1.3.1 的 14 行 scope_in 表、§1.4 的 16 条事实、§1.5 口径解释、§1.6 修订记录）、§2 US-1~US-7、§3 FR-1~FR-11、§4 NFR-1~NFR-7、§5 AC-1~AC-12、§6 成功指标、§7 范围排除齐全；frontmatter 与 cr.md 的 title、target-version=0.36、owner=Ray 全等（已核）；11 FR / 12 AC / 7 US / 7 NFR 的计数与正文逐条一致（已核）。"
  FR可测试性: "通过：AC-1~AC-12 覆盖全部 11 条 FR；两处 CLI 契约（FR-7 gate --mode pre-review、FR-9 review-loop reset）按契约域闭合（命令与 flag、输入约束、stdout/stderr 与错误码、调用者约束、幂等、状态副作用、验收观察点）。B-1 的修复给出可验落点：AC-9⑥ 断言 beginLedgerTransaction 接受 writes.length===1、空 write-set 仍抛 TX_WRITESET_INVALID、4 个既有调用点的事务测试全绿；AC-9② 的 index 回滚机制锚到既有先例（crctl.mjs L1141 的 syncLedgerIndex、L697 的崩溃恢复路径）。权限项 FR-7 为 N/A（本地只读门禁、零写入可重放），FR-9 为既有 TTY 人类在环约束；HTTP 契约域整体 N/A。"
  范围合理性: "通过：§1.3.1 现为 14 行「仓 + 文件」表（新增第 11 行 lib/durable-tx.mjs），与 FR-9 第 3 条、§1.3.2、NFR-1、AC-9⑥ 四向自洽（已核）；multica 仓仍恰为 4 个文件（前缀句限定段已补，S-1 收口）；§1.4 与 §7 的零新增清单（SLO、M1–M8、P50/P90、计数门禁、账本字段、R14 委派 lint、换 YAML 解析器、平台 DB 与 aifirst/agent-import.mjs）未变。"
  与规划对齐: "通过：来源文档 §3.1–§3.7 七组原位修订逐条落到 FR-1~FR-10，来源 §3.8 验收清单在 §5 末尾给出 AC 反向映射；§1.5 对来源 §3.1.5 的解释（活跃合同引用为 0、scripts/test/** 内负向断言字面量不计入）与「须人工审批一并确认」的标注在位，未静默缩小 AC；§1.6 修订 0.1.1 列出本轮全部落点。"
  依赖识别: "通过：§1.4 事实 10 已补齐 ledger write-set 前置条件（lib/durable-tx.mjs:487）、空 write-set 的独立拒绝（:311）、index 回滚机制族（syncLedgerIndex L682、rollbackVersionWrite L2725、rollbackOwnerWrite L2471）与 4 个调用点的文件构成，B-1 指出的缺失项闭合；事实 7 与事实 16 明确 recoverCommand 在本 CR 是「补入」而非「保留」，CR-R 的先决约束在位；FR-7/FR-9 引用的既有先在盘上逐条可复核。"
suggestions:
  - "已解决：B-1 [prd.md §3 FR-9 第 2~7 条；§1.4 事实 10；§1.3.1 第 11 行；AC-9①/②/⑥；§1.6 修订 0.1.1]：FR-9 第 3 条给出显式择一结论——write-set 仅 review-loop.yml，把 tools/skills/shared/crctl/scripts/lib/durable-tx.mjs:487 的 writes.length < 2 原位放宽为 < 1，并写明两条未采用替代（塞入内容不变的第二个文件、绕开 beginLedgerCommand 自建路径）的否决理由。基线逐条复核成立：①:487 原文为 if (!Array.isArray(writes) || writes.length < 2) throw TxError(TX_WRITESET_INVALID, ledger transaction 至少需要两个文件)；②空集另有独立拒绝（applyWriteSet :311 entries.length === 0）；③单文件与多文件路径同构——beginLedgerTransaction 的 writes.map（:492）、applyWriteSet 的逐条 CAS 与 before 快照（:310–360）、rollbackLedgerPayload 的逐条回滚（:429–455）均无长度假设，recoverLedgerTransaction 也按 payload.entries 通用处理；④4 个调用点全部 ≥2 文件（approve crctl.mjs L1130 两文件、owner-set L2535 两文件、version-set L2820–2824 两文件起、review-record L2177–2195 两文件且 bump 再 +1），该放宽对它们零行为差异；⑤全仓仅此一处 length < 2 前置条件（grep 确认），无第二处守卫。index 回滚口径已按既有先例补齐（FR-9 第 5 条：abortLedgerTransaction 按 journal 还原 → syncLedgerIndex(ws, rolled.paths, crctl-review-loop-reset) 使 index 与回滚后内容一致 → 写审计并失败返回；先例 approve L1141、崩溃恢复 L697、rollbackVersionWrite L2725、rollbackOwnerWrite L2471，均已在盘上核对）。AC-9⑥ 给出该放宽的可验断言，AC-9① 的「commit 只含 review-loop.yml」与 FR-9 第 4 条的 AI-First-Tx trailer、commitRequired=true 记账口径一致（先例 approve L1137）。B-1 关闭。"
  - "已解决：S-1（上轮 suggestion，非 blocker）[prd.md §1.3.1]：前缀句后已补限定段，写明该前缀句是公共 Prompt 事实源归位口径、不排除 FR-1 的两份部署副本退役编辑，并给出本 CR 对 ../multica 的改动共 4 个文件、与 AC-11 一致。盘上复核：multica worktree 的 cr-prompts-revised 下 _context.md 命中仍为 dev-agent.md:47 与 quality-reviewer-agent.md:19 两处，CUSTOM.md:387 为 #75 行，四文件口径成立。"
  - "已解决：S-2（上轮 suggestion，非 blocker）[prd.md §1.3.3 与 §3 FR-7 第 4 条、§1.4 事实 7/16]：「兼容字段保留」已改为「补入」，§1.3.3 的不新增限定已收窄为账本字段 / 子命令 / API，事实 7 已写明 gate 错误体 extra 当前为空、该字段名沿用既有 crctl 事务错误体与事务返回的命名，事实 16 已写明是补入不是保留；contractDrift 恒为 true 的定位（不携带区分信息、调用方不得据此反推漂移类型）已写入 FR-7，详述留给 SDD。盘上复核：crctl.mjs 的 fail() L43–47 extra 默认空对象；workspace-transactions.mjs 的 recoverCommand 出现在 L777（registerCr 返回）与 L1572/L1575（TxError extra）等。完整枚举见本轮 S-7。"
  - "已解决：S-3（上轮 suggestion，非 blocker）[prd.md §1.3.3]：四查落点已改为 FR-7、FR-9 与 AC-7、AC-9，本 PRD 中不再出现不存在的 §3.9。"
  - "已解决：S-4（上轮 suggestion，非 blocker）[prd.md §3 FR-9 第 2 条]：expected hash 的取值已改引 review-record 先例（readFileChecked 取原文 + sha256，crctl.mjs L2183、L2193），并写明 readAttempts 只返回状态投影、不返回原文或哈希，事务路径的 CAS 由 ledger 事务按 expectedHash 自行校验、不经过 casWrite。盘上复核：durable-tx.mjs L494–495 按 expectedHash 比对；crctl.mjs L672–677 的 casWrite 不在该路径上。"
  - "范围外：S-5（上轮 suggestion，本轮维持在范围外）[prd.md §1.5 与 §5 AC-2]：AC-2 的计入判定保持人工逐条 + 命中清单证据；把该判定机械化属新 lint / 新规则范围，本 CR 不做，本轮不据此判 block。"
  - "建议（不阻塞，归 write-requirement-prd）：S-6（本轮新增）[prd.md §1.4 事实 10]：括注「durable-tx.test.mjs 只测 write-set entry 校验与 journal 形状」过窄——该文件另有锁获取与竞争矩阵、journal 幂等加载、applyWriteSet 的 redo/幂等/第三值冲突、真实 kill-restart 恢复、recoverWriteSet 定向恢复、cleanupTxBlobs 幂等、checkpoint payload slot 等用例（test/durable-tx.test.mjs L36、L59、L93、L121、L159、L198、L230、L254、L272）。核心断言（无测试守卫该 ≥2 规则）经复核成立——该文件无 beginLedgerTransaction 用例，crctl.test.mjs:1432 只断言源码含 beginLedgerCommand；建议改写为「durable-tx.test.mjs 无 ledger write-set 规模断言」以免读者低估既有回归面。"
  - "建议（不阻塞，归 write-requirement-prd）：S-7（本轮新增）[prd.md §1.4 事实 7]：「该字段名当前只出现在 register/checkpoint/merge/writeback/archive 的事务返回与 TxError extra」的枚举不完整，且其证据清单自身越界——所列 workspace-transactions.mjs:1074 属 syncWorkspaceToTrunk（crctl workspace sync，同文件 L1072–1158），crctl test 的返回也带该字段（同文件 L4186 buildTestResponse）。该事实用于支撑 FR-7 沿用既有命名，结论不受影响（先例反而更多），建议删去「只」字或把枚举写成 register L777/L901、workspace sync L1074/L1118/L1128/L1158、merge L1514/L1572/L1575、checkpoint L1945、writeback L2882、archive L3460、test L4186，使证据清单与枚举一致。"
review-loop:
  current-attempt: 2
subject-file: change-requests/CR-2026-063/prd.md
subject-sha256: 9247c107b1f87b72a5ee4ec5750f2f1da8be784aed04d23f47bb8d153eabda70
