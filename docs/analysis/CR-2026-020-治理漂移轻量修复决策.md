# writeback 治理漂移：轻量修复决策

> 承接：`docs/analysis/CR-2026-020-writeback流水线复盘.md`、`docs/analysis/CR-2026-020-会话工作区漂移复盘.md`
> 日期：2026-08-05
> 决策先行：**不单独开 CR-2026-021**。两份复盘的 13 条建议 + 我一度合成的 8 条 FR，冷眼复审后属过度设计——真正必要的只剩几处小改。收敛为最小修复集，直接在对应仓改（配测试），不套整套 CR 状态机。本文件即决策记录，防止 scope 反弹。

---

## 一、为什么不开 CR

- 搜身后剩的都是小改（一个指针字段、一处 status 输出、一个入口校验、几行错误文案）。CR 的 8-TASK 固定治理成本该省在这类小改上，不该省在改账本工具本身上——但这批已经小到"改 + 测"就够。
- 唯一坚持：涉及 `crctl.mjs`（账本写入权威）的改动**必须配 crctl 测试 + 自评**，只是不走 CR 状态机与人工审批门。
- 纯文档 / 约定项（SDD 模板、fixture 约定、AGENTS.md 纪律）git commit 即足够 CAS。

## 二、做什么（最小集）

| # | 改动 | 落点仓 | 量级 | 状态 |
|---|---|---|---|---|
| 1 | `_backlog.yml` 每条 CR 增 `worktree-branch` 指针，`requirement-register` 写入 | tools（crctl.mjs） | 小 | 待落地 |
| 2 | **改现有 `crctl status`** 读 worktree 分支状态 / 报主工作区与分支分叉，数据源用 #1 指针 | tools（crctl.mjs） | 中 | 待落地 |
| 3 | `advance --to writing-back/archived` 缺 `--spec-id` → 命令入口 `BAD_ARGS` fail-fast | tools（crctl.mjs） | 小 | 待落地 |
| 4 | `GATE_BLOCKED` / `ILLEGAL_LEDGER_STATE` 错误摘要首行带修复指引（缺参 / 应先 advance 到 X） | tools（crctl.mjs） | 小 | 待落地 |
| 5 | `crctl merge-metadata` 统一 emit `{repo,trunk,sha,branch}`（branch 自动填 `requirement/{CR-ID}`）；写回脚本必填集收敛为 `{repo,trunk,sha}` | tools | 小 | **写回侧 CR-020 已完成**，crctl 侧待落地 |
| 6 | SDD 模板加"核实命令"列（涉及已有工具输出形态的断言必列并实跑）；fixture 一律从真实文件采样 | engineering-docs / 约定 | 小 | 待落地 |

主链依赖：#1 指针 → #2 status 消费指针。其余独立。

## 三、不做什么（及原因）

明确记录被砍项，防止后续以"补全"名义反弹：

| 砍掉项 | 原因 |
|---|---|
| **新增 `crctl progress` 子命令**（原方案 FR-2 / 复盘 R2） | 漂移根因只有一条：`crctl status` 读主工作区、显示陈旧 drafting。**修 status 输出即可**，不必把"改一个输出"膨胀成"造一个新命令" |
| **CR 写路径守卫**（原方案 FR-3，我 gold-plating） | 两份复盘都没提，是我加的。为"只发生过一次"的覆盖建写入拦截机制属 speculative robustness；status 不再撒谎后，agent 不会基于陈旧信息动手，守卫无必要 |
| **顺序错误专项修复**（复盘 T3/T4 的 SKILL 文档段） | `ILLEGAL_LEDGER_STATE` 已 fail-clean、<1min 自纠。顺序指引顶多折进 #4 的错误文案，不新增 SKILL 正文 |
| **CI tripwire 基建**（我把验收升级成 CI 断言） | 加码。真实文件采样 + 三字段回归用例（普通 `node:test`）足够，不专门搭基建 |
| **行为层临时兜底**（写前四查 / 模板化参数 / 每次先跑 status） | 复盘自己诊断出的"文档 / 记忆防不住"那类脆弱缓解；#1~#2 落地后无必要 |
| **reconcile 投影对账**（PG 侧） | 不在范围；工具层警告解决行为漂移，reconcile 解决投影一致性，边界在实现时写清、不动其实现 |

## 四、merge-commits 字段契约（保留，硬结论）

回答"实际几个字段合理"——**必填 3 个 `{repo, trunk, sha}`，其余宽进严出**。判据是"能否从落点提交复原"：

| 字段 | 定档 | 理由 |
|---|---|---|
| `repo` | 必填 | 多仓，无从复原 |
| `trunk` | 必填 | trunk 名各仓不同（master / main / custom/main），无从复原 |
| `sha` | 必填 | 落点锚，traceability 的根；缺它整条记录无意义 |
| `branch` | 选填（crctl 自动补） | 恒为 `requirement/{CR-ID}`，由段 key + 命名约定可推导 |
| `source-sha` | 不要求（留存不拒） | = 合并提交第二父，`git` 从 `sha` 可复原 |
| `merged-at` | 不要求（留存不拒） | = `sha` 提交时间，`git` 从 `sha` 可复原 |

**核心契约**：必填集 ≡ 唯一生产者 `crctl merge-metadata` 保证输出的集合。写回脚本"严出"只校验这 3 个，"宽进"接受任意额外字段、绝不 reject 历史 6 字段 / `message` 变体。这才根治 CR-2026-020 那 ~6min bug——bug 本质是"脚本 require 6 / 生产者 emit 3"的契约错位，不是字段数大小。

真实数据分布（核实命令 `grep -A6 'merge-commits:' specs/ai-first-platform/traceability.yml`）：CR-001 四字段（含 message）、CR-002/007/008/009 六字段、CR-019 四字段（含 branch）、CR-020 三字段（crctl 纯输出）。

## 五、落点与后续

- **本决策记录**：`docs/analysis/`（随两份复盘一并提交）。
- **crctl 改动（#1~#5 crctl 侧）**：落 `tools` 仓（phase0-tools / `custom/main`）。当前主工作目录未检出该仓（CR-020 worktree 已清理），待检出后按 §二清单落地 + 补 crctl 测试。
- **AGENTS.md 纪律**：与 #2（`crctl status` 分叉检测）**同批落**——现在写会引用尚不存在的分叉告警，故不提前写，避免文档与实现脱节。
- 待 crctl 侧落地后，本文件"状态"列由"待落地"更新为"已完成"并附提交 sha。
